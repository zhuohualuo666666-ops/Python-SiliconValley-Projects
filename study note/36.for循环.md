<!-- 36.for循环 -->

格式：
for 临时变量 in 可迭代对象：
    要执行的操作1
    要执行的操作2


其中，临时变量可自行设置，执行到此自动创建，
可迭代对象可以为range()，也可以是字符串，
循环次数为可迭代对象内元素个数。

例：
for n in range(1, 11):
    print(f'第{n}次你好啊')


其中range括号里区间左闭右开，遍历1-10


for n in range(10):
    print(f'第{n}次你好啊')

其中range括号里从0开始遍历至9,0-9共10个数字